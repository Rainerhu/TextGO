# 当前需求
（无）

# 已完成需求
1. 增加启用禁用每条规则的能力，支持单独启禁用规则
   - Rule 类型新增 `disabled` 属性
   - matcher.ts 中跳过 disabled 规则
   - 快捷键页面每条规则行增加禁用/启用按钮，禁用后规则行半透明显示

2. 增加在每个启用的快捷键下面，添加文件夹的能力，可将多条规则放在一个文件夹里，触发快捷键后，默认展示文件夹的图标，不展开文件夹下面的规则，鼠标移动上去后在下面一行展开每个规则的图标
   - Rule 类型新增 `group` 字段（文件夹名称）
   - Shortcut 类型新增 `groups` 字段（文件夹配置：图标、显示模式）
   - 新增 `RuleGroup` 类型定义
   - Binder 组件新增"文件夹"输入框，可为规则指定分组名称
   - 工具栏页面重构：同组规则合并为文件夹按钮，支持图标/图标+文本/仅文本三种显示模式
   - 鼠标 hover 文件夹按钮时，在下方一行展开显示组内所有规则
   - 快捷键页面规则行显示分组名称 badge
   - 超出 MAX_VISIBLE_ACTIONS 的项目（含文件夹）进入溢出菜单

4. ai对话框能调节尺寸，通过鼠标拖拽调节尺寸，需要记忆上次调节的尺寸，或者不记忆，通过一个开关设置；默认情况下记忆之前的尺寸；并支持修改设置一个默认的尺寸大小
   - popup 窗口启用 resizable
   - 新增 popupRememberSize / popupSize / popupDefaultSize 持久化状态
   - popup 页面监听 resize 事件自动保存尺寸
   - 通用设置页面新增"弹出窗口"设置组（记忆开关、默认宽高）

5. 可一键复制AI 的输出结果
   - AI 对话模式标题栏新增复制按钮（CopySimpleIcon）
   - 点击后将 AI 响应的原始 Markdown 文本复制到剪贴板
